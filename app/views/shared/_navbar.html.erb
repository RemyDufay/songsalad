<div class="container-logo text-center animate__animated animate__slideInDown">
  <div class="buttons-nvb">

    <div></div>
    <div>
      <%= link_to root_path, class: "logo-nvb d-flex justify-content-center align-item-center" do %>
        <%= image_tag "song salad title navbar.png" %>
      <% end %>
    </div>

    <%# <div style="width: auto"> %>
      <%# <% if action_name == "index" %>
        <%# <%= link_to destroy_user_session_path, class: 'btn btn-warning animate__animated animate__bounceInLeft', id: 'logout', method: :delete do %>
          <%# <i class="fas fa-sign-out-alt"></i> %>
        <%# <% end %>
      <%# <% end %>
    <%# </div> %>
</div>
     <% if @songs_count && @songs_count > 1 %>
      <div class="progress w-75" style="margin: auto; border-radius: 8px" >
          <div class="progress-bar progress-bar-striped progress-bar-animated" role="progressbar" style="width: <%= ((@songs_done_count.to_f / @songs_count.to_f ) * 100).to_i %>%" aria-valuenow="<%= ((@songs_done_count.to_f / @songs_count.to_f ) * 100).to_i %>" aria-valuemin="0" aria-valuemax="100"></div>
      </div>
     <% end  %>
</div>
