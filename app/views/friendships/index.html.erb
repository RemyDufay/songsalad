<div class="container-friendship">
  <%= current_user.name %>
  <h2>Mes Amis</h2>

  <div class="mb-2">leaderbord</div>
  <% if current_user.allfriends.any?  %>
    <% current_user.allfriends.each do |friendship| %>
      <% if friendship.friend.name == current_user.name %>
      <%= friendship.user.name %>
      <% else %>
      <%= friendship.friend.name %>
      <% end %>
    <% end  %>
  <% else  %>
    <p>Ajouter un ami 👇👇 </p>

  <% end  %>

  <div class="mb-5">
    <%= simple_form_for @friendship, url: friendships_path, method: :post, html: { class: 'form-inline' } do |f| %>
      <%= hidden_field_tag "friendship[user_id]", current_user.id %>
      <%= f.input :friend_id, collection: @all_users %>
      <%= f.submit "Ajouter un ami", class: "btn btn-primary" %>
    <% end %>
  </div>

  <div>Demande d'amis </div>
<% @pending.each do |demand|%>
  <%= demand.user.name  %>
  <%= link_to "✔️", accept_friendship_path(demand), :method => 'patch' %>
  <%= link_to "❌", cancel_friendship_path(demand), :method => 'patch' %>
<% end  %>



</div>
