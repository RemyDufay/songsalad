Bravo !

C'était votre <%= current_guest.game_sessions.count %>eme parties.

<% @game_session.game_session_songs.each do |song|%>
Il s'agissait de <%= song.game_song.song.title %> par <%= song.game_song.song.author %> (<%= Guess.where(game_session_song: song).count %> coups )
<% end %>
