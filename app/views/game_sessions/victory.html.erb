<div class="container-victory" data-controller="confettis">

  <div><%= image_tag("bravo.png", width: 200, height: 200, :class => "img-fluid animate__animated animate__bounceInDown") %></div>

  <div class="container d-flex flex-column text-center">
    <div>
      <%= image_tag("victoire.png", width: 100, height: 100, :class => "img-fluid victoire-logo animate__animated animate__bounceInLeft") %>
    </div>
      <%# confettis %>
      <div class="slot" data-confettis-target="slot">
      </div>
  </div> <!-- container -->

  <div id="carouselExampleInterval" class="carousel slide w-75 animate__animated animate__rubberBand " data-bs-ride="carousel">
    <div class="carousel-inner">
      <%# Slide %>
      <% @game_session.game_session_songs.each_with_index do |song, index|%>
        <div class="carousel-item text-center <%= "active" if index == 0 %> bubble-victory-green" data-bs-interval="20000">
          <div class="w-100 mb-2" style="min-height: 9rem;">
            <div class="mt-4 choix stat-category">
              <h1><b>
                <%= song.game_song.song.title %>
              </b></h1>
            </div>
            <div class=" mt-3 choix">
              <h3>
                <%= song.game_song.song.author %>
              </h3>
            </div>
              (<%= Guess.where(game_session_song: song).count %> coups )
          </div>
        </div>
      <% end %>


    </div>

    <% if  @game_session.game_session_songs.count > 1 %>
      <%# button %>

      <%# flèches %>

    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleInterval" data-bs-slide="prev">
      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
      <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleInterval" data-bs-slide="next">
      <span class="carousel-control-next-icon" aria-hidden="true"></span>
      <span class="visually-hidden">Next</span>
    </button>
  </div>
  <% end %>
  </div>




  <div class="mode-de-jeu container text-center animate__animated animate__bounceInUp">
    <div>
      <%= link_to "Retour à l'accueil", root_path, class: "btn d-block btn-primary btn-lg mode-de-jeu btn-victory" %>
    </div>
    <div data-controller="clipboard" data-clipboard-feedback-text-value="Copié dans le presse-papier !" data-clipboard-copied-value="J'ai trouvé la chanson du jour en <%= @guesses_count  %> coups ! 🥗 songsalad.com 🥗 ">
      <button class="btn btn-primary mode-de-jeu" data-action="click->clipboard#copy">Partager mon résultat</button>
    </div>
  </div>


</div> <!-- contqiner victory -->
